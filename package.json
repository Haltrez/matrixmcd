{
  "name": "official-mcdonalds-coin",
  "private": true,
  "description": "Static site + serverless API for tmcd.fun ($MCD). No build step for the frontend; /api functions are deployed automatically by Vercel.",
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  }
}
